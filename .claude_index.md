# .claude_index.md — 全局輕量索引

> 用途：Token 最佳化用的指標表。要找檔案先查這裡，不要靠記憶或全文掃描整個 repo。
> 規範：每列只記「檔案 → 一句話摘要」；需要完整內容時才 Read 對應檔，用完即從工作記憶釋放。
> ⚠️ 本能力範本中的範例（`oTag` 狀態表、`rNo` 無後綴鐵律、`figma_tokens.json`／`bg-primary` 等）只是格式示範，**非本專案實況**；本索引只記真實存在的檔案。

## 進入點與索引

| 檔案 | 一句話摘要 |
| :--- | :--- |
| `CLAUDE.md` | 專案進入點：RAG 路由表＋Token 最佳化規範＋常用速查（team path、Auth 測試）。 |
| `.claude_index.md` | 本檔：全局輕量索引（檔名＋一句話摘要）。 |
| `tree.md` | HackMD 文件樹狀目錄（獨立檔，與 wiki 化無關，平時不載入）。 |
| `.gitignore` | 忽略清單；`.env`（含 `HACKMD_TOKEN`）已排除，token 絕不進 repo。 |

## wiki（依任務動態載入）

| 檔案 | 一句話摘要 |
| :--- | :--- |
| `wiki/hackmd_rules.md` | HackMD API v1 完整參考：端點、欄位、code snippets、Gotchas。 |
| `wiki/recruitment_system_rules.md` | 動筆前求才 vs 求職判斷；業務代碼權威來源 `B1j3sN-bzx`；1HR 整合存證。 |
| `wiki/glossary.md` | 專案術語/縮寫字典：欄位、業務代碼家族、SignalR 名稱、`Type`／`InterViewKind`／`SendKind` 對照。 |
| `wiki/figma_rules.md` | Figma → HackMD 轉換慣例（目前佔位待補）。 |
| `wiki/mermaid_styling_rules.md` | Mermaid 圖表視覺樣式規範（配色、邊框、防跑版）。 |
| `wiki/platform_skills_reference.md` | 本 repo 自製 skill vs 官方內建 skill 的選用判斷。 |

## skills（用 Skill 工具載入）

| 檔案 | 一句話摘要 |
| :--- | :--- |
| `.claude/skills/spec-doc-1111/SKILL.md` | 1111 規格書撰寫慣例：章節編號、MECE 狀態表、🚧 待補區塊、雙視角、版控。 |
| `.claude/skills/spec-doc-1111/assets/template.md` | 求才系統規格書範本。 |
| `.claude/skills/spec-doc-1111/assets/template-jobseeker.md` | 求職系統（求職主網）規格書範本。 |
| `.claude/skills/pm-toolkit/SKILL.md` | PM 工具箱：OTP/MFA 邊界、Mapping Table/正規化、數據分析、跨部門溝通、輸出原則。 |

## HackMD 本機快取（`notes/`）

> 規範：快取檔按文件 note ID 對應，版號以 HackMD 為準（定期 fetch 更新）；**不在此修改內容**，只當唯讀參考。

| 檔案 | Note ID | 版本 | 一句話摘要 |
| :--- | :--- | :--- | :--- |
| `notes/E1-聯絡人才.md` | `cj-xlto2SdOtVskt3TkhdA` | v0.0.7 | E.1 聯絡人才規格書（求才系統，聊天列表＋聊天室＋邀約流程）。 |
| `notes/H3-訊息樣式.md` | `_8_6BHe5Qhu4VXJqaYRKOA` | v0.1.4 | 訊息樣式：一般訊息／邀約訊息（詢問意願／面試邀約／面試異動／取消面試／錄取通知／感謝函）雙視角泡泡樣式＋ SendKind 對照。 |
| `notes/uS9-跨系統流程與後端邏輯.md` | `uS9yE837SYedY9hQFneb6Q` | — | 求才↔求職跨系統後端邏輯：共同發送/回覆行為表、三支查詢 API 應用位置、完整流程圖（flowchart＋循序圖，含四支整併 API 呼叫點、SignalR 常駐連線段）。 |

## API 契約文件（`notes/api/`）

> 鐵律：涉及同一支 API 一律 Read 此處文件取欄位／前端判斷，勿憑記憶或轉抄版；後端出新版時覆蓋同檔名。規則見 `wiki/recruitment_system_rules.md §3`。

| 檔案 | API | 一句話摘要 |
| :--- | :--- | :--- |
| `notes/api/echat-get-detail-infoNo.md` | `GET /api/v1/echat/get-detail/{infoNo}` | 單筆對話完整內容；含 `oJsonB`／`tJsonB` 訊息明細全欄位、Type/SendKind/InterViewKind 前端判斷、**未讀判斷**（`Readflag`／`OViewDate`／`TViewDate`）。 |
| `notes/api/echat-get-echat-mail-logs.md` | `GET /api/v1/external/echat/get-echat-mail-logs` | 聊天列表 API；回傳每筆對話摘要（最後訊息、最後訊息類型、雙方最後查看時間、意願回覆狀態）。 |
| `notes/api/echat-get-by-condition.md` | `GET /api/v1/external/echat/get-by-condition` | 關鍵字／條件搜尋歷史記訊；`sendType` 為陌生訊息判斷依據；查詢層級 `mailType` 與 jsonB `type` 是兩層不同代碼。 |
| `notes/api/echat-update-chatlog.md` | `POST /api/v1/external/echat/update-chatlog` | 信件／即時通異動後的整併同步 API；發事件到 EventBus 合併 `EChatLog`＋`MailNoticeDetail`。 |
| `notes/api/echat-engineering-sequence-original.md` | — | 工程端提供之原始循序圖（前後端技術視角）＋整合進跨系統流程圖時的 4 點修正記錄。 |
| `notes/api/E1-component-showcase.md` | — | 前端 component 狀態機（SDD 文字版）：`chatMessageMapper`／`toMailType`／`toInterviewStatus`、卡片矩陣、渲染優先序、sendKind→sender 對照；前端實作契約。 |
| `notes/api/echat-realtime-push-flow.md` | — | 即時推播真實架構：`eChatHandler.ashx`（kind=5）寫 DB →`update-chatlog`→ EventBus →`apiSendMessage.ashx`（驗簽、查線上狀態、`onSignal`＋FCM/APNS）；含 `senderType` 1企業/2求職者分流、`ContextID` 固定字串、前端忽略 `MsgLog` 改打 API 取全文等細節。循序圖 v7 依此重繪推播段落。 |

> **E.1／信件即時通整併需求**：一律 inference based on 上述 `notes/`＋`notes/api/`＋`.claude/assets/E1/` 這批已交叉驗證的素材；規則與定案結論見 `wiki/recruitment_system_rules.md §6`。

## 資產

> 規範：截圖依所屬規格書放對應子目錄；以 GitHub raw URL（含 commit SHA）嵌入 HackMD，歷史連結永久有效，移動檔案不影響已發布的 HackMD 圖片。

| 路徑 | 所屬文件 | 說明 |
| :--- | :--- | :--- |
| `.claude/assets/E1/` | E.1 聯絡人才 | 聊天室/列表/封鎖/附件/發送通知等介面截圖（共 9 張）。 |
| `.claude/assets/E1/E1_component_showcase.html` | E.1 聯絡人才 | 前端 component 展示網站（`components-dev.1111.com.tw/notify-message-recruit`）渲染快照，含各訊息卡片/狀態的 UI 判斷規則；CSS 已內嵌，JS 未含（原站為內網 -dev 網域，無法直連，改由使用者提供 .mhtml 存檔轉存）。 |
