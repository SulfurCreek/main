# .claude_index.md — 全局輕量索引

> 用途：Token 最佳化用的指標表。要找檔案先查這裡，不要靠記憶或全文掃描整個 repo。
> 規範：每列只記「檔案 → 一句話摘要」；需要完整內容時才 Read 對應檔，用完即從工作記憶釋放。

## 進入點與索引

| 檔案 | 一句話摘要 |
| :--- | :--- |
| `CLAUDE.md` | 專案進入點：RAG 路由表＋Token 最佳化規範＋常用速查（team path、Auth 測試）。 |
| `.claude_index.md` | 本檔：全局輕量索引（檔名＋一句話摘要）。 |
| `tree.md` | HackMD 文件樹狀目錄（獨立檔，與 wiki 化無關，平時不載入）。 |
| `.gitignore` | 忽略清單；`.env`（含 `HACKMD_TOKEN`）已排除，token 絕不進 repo。 |
| `scripts/hackmd_safe_patch.py` | 安全回寫 HackMD note（防覆蓋）：PATCH 前 recheck 遠端＋比對 baseline，遠端被改過就 exit 1 中止不盲蓋；即 `wiki/hackmd_rules.md` 規則 D 的自動化版。 |

## wiki（依任務動態載入）

| 檔案 | 一句話摘要 |
| :--- | :--- |
| `wiki/hackmd_rules.md` | HackMD API v1 核心：認證、team path、**規則 A–D（本地快取＋安全回寫防覆蓋）**、short id 反查、端點一覽表、欄位參考、Status Codes、Gotchas。 |
| `wiki/references/hackmd-api-endpoints.md` | 各端點展開細節（request/response）、Folder API 全套、Node/Python/cURL 範例；**查特定端點或要現寫呼叫時才讀**。 |
| `wiki/recruitment_system_rules.md` | 動筆前求才 vs 求職判斷；業務代碼權威來源 `B1j3sN-bzx`；1HR 整合存證。 |
| `wiki/glossary.md` | 專案術語/縮寫字典：欄位、業務代碼家族、SignalR 名稱、`Type`／`InterViewKind`／`SendKind` 對照。 |
| `wiki/figma_rules.md` | Figma → HackMD 轉換慣例（目前佔位待補）。 |
| `wiki/mermaid_styling_rules.md` | Mermaid 圖表視覺樣式規範（配色、邊框、防跑版）。 |
| `wiki/platform_skills_reference.md` | 本 repo 自製 skill vs 官方內建 skill 的選用判斷。 |

## skills（用 Skill 工具載入）

| 檔案 | 一句話摘要 |
| :--- | :--- |
| `.claude/skills/spec-doc-1111/SKILL.md` | 1111 規格書撰寫骨架：專案判斷、章節編號、初始化分區、MECE 狀態表、🚧 待補區塊、階段拆分、流程圖、交付檢查清單。 |
| `.claude/skills/spec-doc-1111/references/message-cards.md` | 訊息泡泡／邀約卡片雙視角規格；**寫聊天室訊息類文件時才讀**。 |
| `.claude/skills/spec-doc-1111/references/styling.md` | 樣式與符號慣例（紅字、反引號、圖片、摺疊、錨點、版控格式）；**查確切寫法時才讀**。 |
| `.claude/skills/spec-doc-1111/references/cross-doc-refs.md` | 跨文件引用三層方式（行內連結／iframe／全文嵌入）；**要連結或嵌入其他 note 時才讀**。 |
| `.claude/skills/spec-doc-1111/assets/template.md` | 求才系統規格書範本。 |
| `.claude/skills/spec-doc-1111/assets/template-jobseeker.md` | 求職系統（求職主網）規格書範本。 |
| `.claude/skills/pm-toolkit/SKILL.md` | PM 工具箱：OTP/MFA 邊界、Mapping Table/正規化、數據分析、跨部門溝通、輸出原則。 |

## HackMD 本機快取（`notes/`）

> 規範：快取檔按文件 note ID 對應，版號以 HackMD 為準（定期 fetch 更新）；**不在此修改內容**，只當唯讀參考。

| 檔案 | Note ID | 版本 | 一句話摘要 |
| :--- | :--- | :--- | :--- |
| `notes/E1-聯絡人才.md` | `cj-xlto2SdOtVskt3TkhdA` | v0.1.8 | E.1 聯絡人才規格書（求才系統，聊天列表＋聊天室＋邀約流程）；列表載入＋§1.2.1 搜尋皆走 `get-by-condition`（`get-echat-mail-logs` 已棄用；`cursor`=`{organNo}_{RNo}`＋`limit` 分頁＋篩選）；§1.3 已讀規則（廠商進聊天室即整室未讀求職者訊息標記已讀，不判斷捲動）＋紅點規則（進聊天室後該室全部未讀狀態消除，左側列表紅點同步消失）＋接收參數 `onSignal` 帶 `infoNo`/`bNo`；§1.3.2 查看履歷：一般邏輯（`eChatHandler.ashx kind:4` AJAX＋`ResumeLink` URL 組裝、三種情境判斷）直接展開，**只有扣點機制**（`kind:11`）收在可收合區塊、備註之後要實作；含小聶 v0.1.7 進 QA 前 review（功能入口／進入畫面章節重整）；API 附錄含 `oJsonB`/`tJsonB` 全欄位（已移除 `FileName`/`FilePath`）。 |
| `notes/H3-訊息樣式.md` | `_8_6BHe5Qhu4VXJqaYRKOA` | v0.1.6 | 訊息樣式：一般訊息／邀約訊息（詢問意願／面試邀約／面試異動／取消面試／錄取通知／感謝函）雙視角泡泡樣式＋ SendKind 對照；面試資訊欄位已標對應 API 參數。 |
| `notes/B21-新增帳號.md` | `W4g5G4LLS-ypAc0goQBdag` | v0.3.8 | B.2.1／B.2.2／B.2.3 新增帳號、修改個人/他人帳號；含 **B.2.1.4 新增帳號modal**（新增方式卡片、快速新增之 email／帳號／權限／群組規則、複製帳號、4.1／4.2 Buttons）；截圖以 GitHub raw URL 嵌入，存於 `.claude/assets/B2/`。 |
| `notes/uS9-跨系統流程與後端邏輯.md` | `uS9yE837SYedY9hQFneb6Q` | v1.0 | 求才↔求職跨系統後端邏輯（2026-08 全文重構、版號重啟）：§1 API 總覽（**3 支查詢＋1 支同步 API**：`get-by-condition` 列表＋搜尋、**`get-by-condition-count` 筆數統計（新）**、`get-detail/{infoNo}`、`update-chatlog`；篩選條件表、**兩種 cursor**——列表 `{organNo}_{RNo}` vs 明細 `{organNo}_{bNo}`、count 回傳 9 欄、`get-detail` 巢狀 `AttachFiles`/`Calendar`/`E2Calendar`）；§2–4 共同發送/回覆行為與類型對照；§5–6 完整流程圖＋循序圖；§7 SignalR 機制（連線時機、新版業務流程、update-chatlog 內部 EventBus+Callback 架構、**§7.4 `notifyType` 0更新/1已讀/2刪除/3收回/4星號 與各自通知行為**、常用動作對照表）；2 處 🚧 待確認（`get-detail` cursor 格式表述不一致、`notifyType=4` 通知行為未載明）。 |

## API 契約文件（`notes/api/`）

> 鐵律：涉及同一支 API 一律 Read 此處文件取欄位／前端判斷，勿憑記憶或轉抄版；後端出新版時覆蓋同檔名。規則見 `wiki/recruitment_system_rules.md §3`。

| 檔案 | API | 一句話摘要 |
| :--- | :--- | :--- |
| `notes/api/echat-get-detail-infoNo.md` | `GET /api/v1/echat/get-detail/{infoNo}` | 單筆對話完整內容；含 `oJsonB`／`tJsonB` 訊息明細全欄位、Type/SendKind/InterViewKind 前端判斷、**未讀判斷**（`Readflag`／`OViewDate`／`TViewDate`）。 |
| `notes/api/echat-get-echat-mail-logs.md` | `GET .../get-echat-mail-logs` | **⚠️ 已棄用（2026/07）**：列表載入改用 `get-by-condition`；本檔保留歷史對照。 |
| `notes/api/echat-get-by-condition.md` | `GET /api/v1/echat/get-by-condition` | **兼列表載入＋關鍵字/條件搜尋**（取代 mail-logs）；回傳對話摘要**陣列**（不再回 JsonB，明細走 get-detail）；cursor=`{organNo}_{RNo}`；`sendType` 為陌生訊息判斷依據；查詢層級 `mailType` 與 get-detail JsonB `type` 兩層不同代碼。 |
| `notes/api/echat-update-chatlog.md` | `POST /api/v1/external/echat/update-chatlog` | 信件／即時通異動後的整併同步 API；發事件到 EventBus 合併 `EChatLog`＋`MailNoticeDetail`；**非同步 fire-and-forget**——立即回 `200 true`，實際整併走 `MergeRecruitMailChat` 事件→`MergeRecruitMailChatEventHandler` callback→`SyncEChatMailLogJsonBAsync` 寫入 `EChatMailMerge` 資料表，呼叫端需重新查詢才看到最新狀態。 |
| `notes/api/echat-engineering-sequence-original.md` | — | 工程端提供之原始循序圖（前後端技術視角）＋整合進跨系統流程圖時的 4 點修正記錄。 |
| `notes/api/E1-component-showcase.md` | — | 前端 component 狀態機（SDD 文字版）：`chatMessageMapper`／`toMailType`／`toInterviewStatus`、卡片矩陣、渲染優先序、sendKind→sender 對照；前端實作契約。 |
| `notes/api/echat-realtime-push-flow.md` | — | 即時推播真實架構：`eChatHandler.ashx`（kind=5）寫 DB →`update-chatlog`→ EventBus →`apiSendMessage.ashx`（驗簽、查線上狀態、`onSignal`＋FCM/APNS）；含 `senderType` 1企業/2求職者分流、`ContextID` 固定字串、前端忽略 `MsgLog` 改打 API 取全文等細節。循序圖 v7 依此重繪推播段落。 |

> **E.1／信件即時通整併需求**：一律 inference based on 上述 `notes/`＋`notes/api/`＋`.claude/assets/E1/` 這批已交叉驗證的素材；規則與定案結論見 `wiki/recruitment_system_rules.md §6`。

## 資產

> 規範：截圖依所屬規格書放對應子目錄；以 GitHub raw URL（含 commit SHA）嵌入 HackMD，歷史連結永久有效，移動檔案不影響已發布的 HackMD 圖片。

| 路徑 | 所屬文件 | 說明 |
| :--- | :--- | :--- |
| `.claude/assets/E1/` | E.1 聯絡人才 | 聊天室/列表/封鎖/附件/發送通知等介面截圖（共 9 張）；`diagrams/` 子目錄存循序圖 PNG 渲染快照。 |
| `.claude/assets/E1/E1_component_showcase.html` | E.1 聯絡人才 | 前端 component 展示網站（`components-dev.1111.com.tw/notify-message-recruit`）渲染快照，含各訊息卡片/狀態的 UI 判斷規則；CSS 已內嵌，JS 未含（原站為內網 -dev 網域，無法直連，改由使用者提供 .mhtml 存檔轉存）。 |
| `.claude/assets/B2/` | B.2.1 新增帳號 | B.2.1.4 新增帳號 modal 介面截圖（11 張）；自 AxShare 原型 `.mhtml` 抽出的原生 PNG，非重新截圖。 |
