<!--markdownlint-disable MD033-->
<!--markdownlint-disable MD013-->

# {代號} {功能名稱}

User Story: `身為一個 {Persona}，我想要 {做什麼}，以便 {達成什麼目標}`
Use Case: `{調用端＋執行動作＋受影響標的物}`

:::spoiler Document info

文件版本：v0.1.0
最後更新：{yyyy/mm/dd}
文件作者：{作者}
文件狀態： <span style="color:gray">草稿</span></h6>
功能位置：
<{該功能的 URL}>
<!-- 尚無確定 URL 時直接寫 TBD，不要放推測網址 -->

## 版控紀錄

| 版本 | 日期 | 作者 | 調整說明（異動區段 + 內容摘要） |
| :---: | :--- | :--- | :--- |
| 0.1 | {yyyy-mm-dd} | {作者} | 初版 |
<!-- 新版本一律 append 在最下面一列（時間升冪，最新版在最末列），不可插中間或倒序 -->

[TOC]

:::

## 示意圖

{直接嵌入 wireframe 或流程示意圖，禁止放外部 URL。沒有則標示：`待補`}

---

## 初始化

### 進入路徑

* {入口一：導覽路徑或跨文件連結；截圖緊貼此 bullet 下方}
* {入口二}

（直接條列各入口，不加「路徑 A／B」標籤；若只有一個進入點，刪除此節，直接在 初始化 intro 說明）

### 權限判斷

進入頁面時，依序驗證以下條件（代碼定義見 [求才系統權限代碼表]({noteId})）：

| 權限條件 | 代碼 | 不符合時的處理 |
| :--- | :---: | :--- |
| 廠商需為 VIP | `oStatus = 1` | 顯示 Alert：「貴公司目前非VIP會員...」，點擊後返回首頁 |
| 帳號需有 XX 管理權限 | `代碼XX` | 顯示無權限 Alert，點擊後返回首頁 |

### 資料載入

* 進入畫面時載入：{API 名稱或 DB 查詢說明}
  * 預設排序：{說明}
  * 預設篩選條件：{說明，無則標示「不帶任何篩選條件」}
* Responsive 斷點：{如有則填寫，無則刪除此行}

---

## 情境說明

{若初始化條件會造成整頁樣式大幅改變（例如：VIP 狀態不同、頁面有無資料），在此展開各情境說明。}
{若不需要，刪除此節。}

### 情境 A：{情境名稱}

### 情境 B：{情境名稱}

---

## 1. {第一個區塊名稱}

{針對頁面上第一個大區塊進行說明，例如：頁面標題列、篩選條件區。}

### {廠商有資料情境}

![image]({全寬示意圖 URL})

* {說明}
* 判斷{對象}{狀態}：
  * {條件1}時：
    * {結果1}
  * {條件2}時：
    * {結果2}

#### 1-1 {子區塊名稱}

| 狀態 | 觸發條件 | 畫面呈現 |
| :--- | :--- | :--- |
| 載入中 | 資料尚未回傳 | 顯示 loading 動畫 |
| 有資料 | API 正常回傳 | {描述正常畫面} |
| 無資料 | API 回傳空陣列 | {Empty State 文案 + 排版間距，例：距上方篩選區 160} |
| 錯誤 | API 失敗 / 權限不符 | 顯示 toast：「{錯誤文案}」 |

**1-1-1 {元件細項}**
  * 判斷{狀態}：
    * 當{條件}時，顯示為 default 樣式（黑色）
    * 當{條件}時，顯示為 Error 樣式（紅色）
    * 當{條件}時，顯示為 disabled 樣式

---

## 2 {第二個區塊名稱}

### {情境}

|{標頭：每筆顯示為一個 row}|滑鼠進入時，顯示為 hover 樣式|
|---|--|
|{欄位}|{行為說明，多條件用 <ul><li>...</li></ul>}|
|{狀態欄位}|判斷機制：`{欄位}-{條件}`<br><ul><li>`status = 0,2,4,5,6` 時，固定顯示為 disabled 狀態</li><li>其餘狀態時，依{次要條件}判斷：<ul><li>{條件A}：{結果A}</li><li>{條件B}：{結果B}</li></ul></li></ul>|

### {區塊} 流程圖

```mermaid
flowchart TD
  Start([開始]) --> Check{判斷{狀態}}
  Check -->|{條件A}| A[{情境A}]
  Check -->|{條件B}| B[{情境B}]

  classDef condition fill:#e1f5fe,stroke:#01579b,stroke-width:2px;
  classDef action fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px;
  classDef alert fill:#ffebee,stroke:#c62828,stroke-width:2px;
  classDef block fill:#f5f5f5,stroke:#666,stroke-width:1px,stroke-dasharray: 5 5;
  class Check condition;
```
